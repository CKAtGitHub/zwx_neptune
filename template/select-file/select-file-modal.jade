div
    div.modal-header
        button.close(type='button',ng-click="vm.cancel()",aria-label="关闭")
            span &times
        h4.modal-title 选择
        p(ng-show="vm.refresh.$$state.status === 0") 正在加载数据,请稍后...
    div.modal-body
        div.row
            div.col-md-12
                p 已经选择了{{vm.selectNum}}个文件.
            div.row(ng-repeat="rows in vm.files",style="padding:5px;")
                div.col-xs-12.col-md-12(ng-repeat="row in rows")
                    div.list-group(ng-click="vm.selectFile(row)")
                        div.list-group-item(href="")
                            span.glyphicon.glyphicon-ok.bg-primary(style="position:absolute;right:25px;top:5px;",ng-show="row.selected")
                            div.list-group-item-heading
                                h4 {{row.data.name}}
                            p {{row.data.createdate | timestampFilter}}
                            p.list-group-item-text {{row.data.remark}}
    div.modal-footer
        button.btn.btn-primary(type="button",ng-click="vm.ok()") 确定
        button.btn.btn-warning(type="button",ng-click="vm.cancel()") 取消